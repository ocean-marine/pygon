"""Pygon project main package.

Root package emphasizing explicit error handling, type safety, and human-AI collaboration.

Core modules: types/ (Result patterns), models/ (immutable dataclasses), validators/ (input validation), 
services/ (business logic), repositories/ (data access), protocols/ (interfaces), config/ (settings), 
utils/ (helpers), examples/ (reference implementations).

Pygon principles: explicit error handling via Result types, single-responsibility functions,
immutable data structures, comprehensive type annotations.
"""